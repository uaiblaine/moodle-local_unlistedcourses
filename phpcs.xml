<?xml version="1.0"?>
<ruleset name="{{COMPONENT}}">
    <description>PHPCS rules for {{COMPONENT}}.</description>

    <rule ref="moodle" />

    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/vendor/*</exclude-pattern>
</ruleset>
